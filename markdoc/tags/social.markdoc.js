import { Social } from '@/components'

export default {
  render: Social,
  attributes: {
    className: { type: String },
    icon: { type: String },
    name: { type: String },
    url: { type: String }
  }
}
